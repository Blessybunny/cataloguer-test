BLADE STATUS:
    - dashboard:
        - manager                   - clean | has db
        - students                  - wip | has db
    - primary layouts:
        - master                    - clean
        - page                      - clean
    - dashboard layouts:
        - permanent-form-back       - wip | no db
        - permanent-form-front      - wip | no db
        - prompt                    - wip | no db
        - report-card-back          - wip | has db
        - report-card-front         - wip | has db
    - login:

BLADE STRUCTURE:
    - master:
        - yield: title
        - yield: head
        - yield: master-content
    - page:
        - yield: content

						@php
							$date = new DateTime($student->li_birthdate);
							$now = new DateTime();
							$interval = $now->diff($date);

							echo $interval->y;
						@endphp